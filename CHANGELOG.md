# سجل التغييرات (CHANGELOG)

## [2026-08-22]
- refactor: تفكيك وحدة التنسيقات المساعدة `shared/css/utilities.css` إلى 8 وحدات تخصصية دقيقة داخل `shared/css/utilities/` (`layout`, `spacing`, `text`, `colors`, `borders`, `effects`, `buttons`, `responsive`) وصيانتها عبر مجمّع فرعي.
- refactor: تفكيك ملف التنسيقات المشترك `shared/core.css` إلى 9 وحدات تخصصية مستقلة داخل `shared/css/` مع الاحتفاظ بـ `core.css` كمجمّع رئيسي عبر `@import` لضمان استقلال المنظومة والتوافق التام (100% Offline-First).
- feat: إضافة سكربت التدقيق الآلي للأصول `validate_assets.py` لفحص الوجود الفيزيائي للملفات، والتحقق الصياغي للأقواس (CSS Linting)، ومطابقة استيرادات المجمّع وروابط الدروس.
- fix: إصلاح خطأ صياغي في مصفوفة `rulesData` بملف `pages/31.html`، وضبط وسوم إغلاق النصوص لتفادي توقف مفسر الجافا سكريبت.
- feat: ترقية وصولية نمط المراجعة الذكية للأخطاء (`renderReview`) في `shared/app.js` عبر تزويد بطاقة الكلمة المسترجعة بوسم `aria-label` ونقل التركيز البرمجي (`focus`) لقارئات الشاشة.
- refactor: تنقية شفرة `shared/ui-template.js` بإزالة استدعاء DOM غير المجدي قبل الحقن، وتحديث وثيقة `project_state.md` لمطابقة البنية المفككة الحالية.
- chore: تطهير الأكواد الميتة (Dead Code Elimination) على 5 مراحل: حذف الدالات الصوتية المهملة (`nav`, `tap`, `flip`, `drop`)، وحذف متغير `wordRenderTime`، وتطهير فئات الألوان غير المستخدمة (`color-3`, `color-4`, `color-5`)، وحذف ثوابت الفهرس المهملة.
- refactor: تنقية ملف التنسيق `core.css` واختزال الكتل الكبيرة المتكررة: توحيد متغيرات منحنيات الحركة والانتقال (`--ease-bounce` و `--ease-standard`)، ودمج كتل حركات Connect 4، وتوحيد قواعد الحدود الصلبة وفئات الانتقال الحركي.
- refactor: تنقية ملف التنسيق `core.css` واختزال الخصائص المتكررة: دمج هيكل بطاقات الكلمات المجزأة والكاملة، وتوحيد قواعد السمات اللونية (`theme-*`) عبر متغيرات CSS، وضبط أوزان الخطوط وقواعد البداية.
- refactor: توحيد الدوال المشتركة (Function Deduplication): دمج دالتي الإشعار في `triggerFeedback`، واستحداث `setGameResumeState` لأزرار ألعاب الاستراحة، ومركزة `updateProgress` لشريط التقدم، وتطهير مرادفات `Sound.playChime`.
- refactor: تطهير الأكواد المكررة وتوحيد دالات التطهير (setSafeHTML) ورسم الكلمات (renderWordInto) والخلط العشوائي (shuffle) على محرك app المركزي تحقيقاً لمبدأ DRY بنسبة 100%.
- feat: إضافة مظهر «الستار» لغرفة ألعاب الكلمات مع بناء قالب «سلَّم الارتقاء» (Mastery Ladder) التفاعلي المنضبط شرعياً والمدعوم صوتياً وتوافقياً مع قارئات الشاشة (ARIA) والمعلم الكفيف.

- feat: توحيد عناوين المنظومة لتتبع نسق `Nour Al-Bayan - Page {N}` في وسوم العناوين والفهرس وتهيئة الدروس الـ28 بعناوين تفصيلية واضحة ومطابقة.
- feat: ترقية المنظومة لتتوافق مع معايير الوصول الشامل (WCAG 2.1 AA / A11Y): إدراج ARIA Live Regions لنطق الكلمات آلياً للمكفوفين، وتحويل عناصر الألعاب كافة إلى أزرار دلالية ذات تسميات ديناميكية، وتوسيع مؤشرات التركيز `:focus-visible` للوحة المفاتيح.
- fix: معالجة دالة `setSafeHTML` في `app.js`، وتصحيح نصوص التذييل في الدروس (21-30)، وتأكيد خلو الصفحات الـ28 من الأخطاء عبر الفحص البرمجي للمتصفح.
- feat: إضافة خيار التحكم في استراحات الألعاب أثناء القراءة (`Mid-Lesson Game Breaks Toggle`)، وحل مشكلة التمرير في جدار الكلمات، وضبط تباعد الخطوط، وإدراج `.nojekyll`.
- feat: تفكيك ملف `shared/core.js` إلى 7 وحدات برمجية مستقلة ومتخصصة (`sound.js`, `ui-template.js`, `app.js`, `games-wordwall.js`, `games-board.js`, `games-extra.js`, `rule-manager.js`) وتحديث صفحات الدروس الـ28.
- feat: تحويل المنظومة بالكامل إلى بنية مستقلة دون إنترنت (Offline-First) عبر استبدال Play CDN بملف core.css المترجم وتوطين مكتبات Chart.js وConfetti والخطوط القرآنية في shared/vendor/.
- feat: توحيد أسماء صفحات الدروس لتصبح بأرقامها فقط (10.html إلى 37.html) وإعادة بناء الفهرس index.html بشبكة بطاقات تفاعلية متجاوبة.
- feat: إدراج دعم اختصارات لوحة المفاتيح والوصول الشامل (ARIA) وشريط التقدم الديناميكي وهندسة الصوت السلسة ونمط المراجعة الذكية للأخطاء (Mistake Review Mode).
- fix: إصلاح عرض البطاقات المقسمة وتطوير التجاوب البصري لكافة الشاشات وإدراج بطاقة المطور والدعاء في القائمة الرئيسية.
- fix: مراجعة شاملة لجميع الألعاب وإصلاح تكرار الأيقونة في لعبة الذاكرة ومعالجة حالات التعادل في XO و Connect 4 وضبط تباين عجلة الحظ.

## [2026-08-21]
- feat: تجريد وربط 9 صفحات جديدة (الدروس 10 إلى 18) بالنواة المشتركة وتوسيع المحرك ليشمل كافة أنماط عرض الكلمات وتحديث الفهرس إلى 28 صفحة.
- docs: تحديث فهرس `index.html` ليعتمد أسماء الملفات مباشرةً في قائمة الدروس وتطهير كافة الملفات المؤقتة.
- refactor: استخلاص هيكل واجهة المستخدم ومنطق الألعاب المشترك كاملاً إلى `shared/core.js` وتجريد الصفحات الـ19 لتصبح ملفات بيانات إعلانية خفيفة (~160KB إجمالي بدلاً من 1.4MB).
