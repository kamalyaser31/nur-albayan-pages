/**
 * Nour Al-Bayan - English Language Pack (en)
 * English UI Localization Pack
 */

(function() {
    const meta = {
        name: 'English',
        nativeName: 'English',
        dir: 'ltr'
    };

    const strings = {
        // UI & System keys go here
    };

    if (typeof i18n !== 'undefined') {
        i18n.register('en', meta, strings);
    }
})();
